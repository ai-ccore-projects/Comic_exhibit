{
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/main.py"
        }
    ],
    "functions": {
        "api/**/*.py": {
            "excludeFiles": "{public,src,dist}/**"
        }
    }
}